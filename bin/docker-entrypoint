#!/bin/bash -e

mkdir -p /rails/storage /rails/log /rails/tmp/pids /rails/tmp/cache /rails/tmp/sockets
chown -R rails:rails /rails/storage /rails/log /rails/tmp

if [[ "$*" == *"server"* ]]; then
  gosu rails:rails ./bin/rails db:prepare
fi

exec gosu rails:rails "$@"
